name: Data Service CI/CD

on:
  push:
    paths:
      - 'data-service/**'
      - '.github/workflows/data-service.yml'
  pull_request:
    paths:
      - 'data-service/**'

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Run unit tests (H2 in-memory, no MySQL needed)
        working-directory: data-service
        run: mvn test --no-transfer-progress -Dspring.profiles.active=test

      - name: Build JAR
        working-directory: data-service
        run: mvn package -DskipTests --no-transfer-progress

      - name: Build Docker image
        working-directory: data-service
        run: docker build -t legends/data-service:${{ github.sha }} .

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Push Docker image
        run: |
          docker tag legends/data-service:${{ github.sha }} ${{ secrets.DOCKERHUB_USERNAME }}/data-service:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/data-service:latest
