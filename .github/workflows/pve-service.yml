name: PvE Service CI/CD

on:
  push:
    paths:
      - 'pve-service/**'
      - '.github/workflows/pve-service.yml'
  pull_request:
    paths:
      - 'pve-service/**'

jobs:
  test-and-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Run unit tests
        working-directory: pve-service
        run: mvn test --no-transfer-progress

      - name: Build JAR
        working-directory: pve-service
        run: mvn package -DskipTests --no-transfer-progress

      - name: Build Docker image
        working-directory: pve-service
        run: docker build -t legends/pve-service:${{ github.sha }} .

      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #
      # - name: Push Docker image
      #   run: |
      #     docker tag legends/pve-service:${{ github.sha }} ${{ secrets.DOCKERHUB_USERNAME }}/pve-service:latest
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/pve-service:latest
