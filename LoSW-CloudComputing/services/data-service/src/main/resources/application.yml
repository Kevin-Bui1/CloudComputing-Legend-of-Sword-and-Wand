server:
  port: 5003

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/legends}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:password}
    driver-class-name: com.mysql.cj.jdbc.Driver
